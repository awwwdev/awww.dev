# EXHIBIT B

## TASKS

### Total Type Safety and Type Checking the project [est. 8 hours Hamid]

✅ Setting up supabase Cli Extracting Data types from supabase
🟦 Documenting How to update db types after db schema changes
🟡 Adding all missing Types to all components, functions
✅ Onboarding Mehdi for maintaining types
✅ Add automated Linting and Type Checking Tests before commits
✅ Add Build Automated tests before Commit Push to Production

### Reactor Frontend + Optimize Website Speed [est. 6 hours Hamid]

✅ Use Common Nested Layout workaround + any cleanup necessary. This would organize code further and prepare it for scaling up.
(\*Note: this Architecture was released recently. If it were released earlier, the project would be started with that as it provides better structure to organize codes, specially for nested layouts in dashboards)
✅ Experiment with Server-Side-Rendering (SSR) , Server-less function and check it would improve [est. 2 hours]
✅ Implement the best strategy [est. 1 hour]
Research a strategy to cache fetch data and share reusable cache between pages [est. 3 hours]

✅ replace useGet... with useGetR
✅ replace <Table > with <TableR>
✅ replace <RenderQ> with renderNoData()

### Make Payer and Teacher dashboard responsive

Admin Dashboard will not be responsive
✅ Make the website template responsive without improving tables UX on phone [est. 3 hours]
✅ Creating a drawer menu for website
✅ Test all pages and fix overflow bugs [Mehdi]
✅ Test all forms in mobile [Mehdi]
✅ Fitting current table for mobile screens (fixing paginations and top controls overflows) [Mehdi]

### Complete Teacher Dashboard [Mehdi]

✅ View Table his/her sessions by month
✅ View table of sessions for each course and every session is from which package
✅ View table of his/her topics
✅ View table of his/her expertise
🟡 View table of his/her courses ( and students in that course)
🟦View table of the details for each course application
✅ View table of packages purchased by course
🟦View table of payments received + total amount received
✅ View table of the notes for each introductory sessions

🟦 Form: Insert every session after session held/canceled (and its reason, if canceled)
🟦 Every session should be approved by admins

### Complete Admin Dashboard [Mehdi]

🟦 Create + Update + Delete Topics
🟦 Create + Update + Delete: Add Expertise
🟦 Create + Update + Delete: Add new Gift Record
🟦 Create + Update + Delete: Add Rate Conversion
🟦 Create + Update + Delete: Payments to Teachers
🟦 Create + Update + Delete: Add New Course
🟦 Create + Update + Delete: Add Promotion Code Record
🟦 Create + Update + Delete: Add Payment
🟦 (Create Form will be redundant in future as payment by payers are automatically recorded)
🟦 Create + Update + Delete: Add Purchased Package to the database (commision percentage)
🟦 (Create Form will be redundant in future after full automation)
🟦 Create + Update + Delete: Add session
🟦 (Create Form will be redundant after when teachers add sessions in their own dashboards)
🟦 View table which every intro session’s status be determined

Note: forms in this phase are simple forms with some combobox (input + dropbox) to easily search and select foreign keys by name, email or other fields. If any advanced data entry UI such as editable rows inside tables are needed, it is considered as a separate feature.

### Supervise and Revise Deliverable Codes [est. 16 hours Hamid]

✅ Support Mehdi throughout his tasks [est. 5 hours (10 x 0.5 hour sessions)]
✅ Onboarding Mehdi with advanced tools and approaches that will be used [est. 3 hours]
✅ Revising and Refactoring and Solving any Complicated Issue the codes before Delivering [8 hours (One full day if not more before project’s end)]

## Things Exhibit B does not include

- Any functionality that require Supabase storage (like avatar image or any user specific media) and Supabase real-time (like chats functionality)
- Multilingual Features
- Designing and fine-tuning UI (Color pallet, gradients, typography, look and feel, ...) (However, we will use pretty good basics and best practices for establishing UI)
- SEO improvements (The Website in Phase 2 does not need SEO as its all behind the login wall)
- Connecting database to any other service, api, software or product (therefor connecting database to update google sheets is not in the scope of phase 1)
- Any service related to data science, data mining or finding patterns in data, recommendation algorithms.
- Turning the website to PWA.
- Any automated testing such as End-to-End testing, Integration testing, Unit testing. All testing will be done by manually interacting with the developed website.
- Adding marketing and analytics features
- Automated email and push notifications
- Payment Integration
- Guaranteeing that the dashboards works correctly any browser other than any unsupported browser such as any browser on smaller screen over a usual laptop, IE 11 and older, Safari on macOS and iOS (due to its lack of support for modern js features. Although Apple is working hard to catch up on the future versions of Safari.) and all iOS browsers (which are Safari under the hood)

## Browser Support

Expanding Browser support needs more testing and work. Phase 2 is about dashboards behind the login wall. Although it will be used by teachers and players too, for faster development we limit browser support as below.

Note that Browser support can be expanded to include more major browsers and also mobile browsers in future phases. We don’t recommend to include older versions of browsers as the latest features in js and css will make new possibilities and save development time.

It is the client's responsibility to recommend all users of dashboards to use one the supported browsers. (needless to say a list of supported browsers can be shown in entry points of the website)

Note it doesn’t mean it wouldn’t work on other browsers, it means in case a feature is working in all supported browsers it is considered as working, whether it would work or not on other browsers or older versions. Also supporting IE 11 and older is close to impossible.

### Supported Browsers

Latest versions of Firefox and Chrome on Windows 11 and macOS

Note: Previously Chrome had issues with rendering large tables, but hasn’t been observed recently. In case this issue appears again, the support for Chrome will be dropped.
Please see https://bugs.chromium.org/p/chromium/issues/detail?id=461877
Nevertheless Firefox still render tables faster and might give a slightly faster browsing in dashboards

— End of Exhibit B —
